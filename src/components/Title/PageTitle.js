import React from "react";

const PageTitle = ({ title }) => {
  return (
    <div className="l-title mb-5">
      <h1 className="title-page">{title}</h1>
    </div>
  );
};

export default PageTitle;
